function numberPyramids(input){
    let n = Number(input[0]);

    for (let rows = 1; rows <= n; rows++) {
        for (let cols = 1; cols <= n; cols++) {
            const element = array[cols];
            
        }
        
    }
}

numberPyramids(["7"]);
numberPyramids(["12"]);
numberPyramids(["15"]);
