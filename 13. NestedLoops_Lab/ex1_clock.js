function clock(input){
    
    for (let hours = 0; hours < 24; hours++) {
        for (let minutes = 0; minutes < array.length; minutes++) {
           // console.log(`${hours}:${minutes}`);
            if (minutes < 10) {
                console.log();
            }
        }
        
    }
} 

clock();